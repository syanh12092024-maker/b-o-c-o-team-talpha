# 📁 Project: T1

> Auto-generated by FAOS v6 Setup — 2026-03-02
> **Leader:** @pdanh259

## Directory Structure

```
app/projects/t1/
├── sync/           # Data sync/ETL pipelines
├── components/     # React UI components
├── hooks/          # Custom React hooks
├── types/          # TypeScript type definitions
├── utils/          # Utility functions
└── README.md       # This file

sql/t1/
├── tables/         # BigQuery table DDL
└── views/          # BigQuery view definitions

api/routers/t1/
└── ...             # FastAPI router endpoints

sync/t1/
└── ...             # Sync pipelines

docs/designs/t1/
└── ...             # Design documents (filled templates)
```

## Getting Started

1. Đọc `docs/LEADER_QUICK_START.md` (hướng dẫn 5 bước)
2. Chạy `scripts/auto_setup_env.sh` (nhập tên dự án: `t1`)
3. Copy `config/LEADER_ENV_TEMPLATE.env` → `.env.project` → điền credentials
4. Paste `docs/LEADER_AI_ONBOARDING_PROMPT.md` vào Claude/Cursor
5. Điền form thiết kế `docs/STANDARD_TEMPLATES.md` → gửi Boss duyệt → code

## Tech Stack
- **Backend:** Python 3.12 + FastAPI
- **Frontend:** Next.js 15 + React 19 + Recharts
- **Database:** BigQuery (Dataset: `T1_Dataset`)
- **AI:** Gemini 2.0 Flash / GPT-4o (fallback)
