---
name: docker-wizard
description: Auto-generate Dockerfile & Compose configs.
---

# Docker Wizard

## Purpose
Instantly create production-ready `Dockerfile` and `docker-compose.yml` files for standard stacks.

## Usage
```bash
python .agent/skills/docker-wizard/scripts/docker_gen.py --stack node --db postgres
```
