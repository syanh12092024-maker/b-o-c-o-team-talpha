# ═══ Python ═══
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# ═══ Virtual Environments ═══
.venv/
.venv_*/
venv/

# ═══ Secrets & Credentials ═══
.env
.env.*
!.env.example
.env.project
credentials.json
token.json
gmail_credentials.json
gmail_token.json
bigquery_key.json
bigquery_key.json.*
bigquery_key_*.json
*.pem
*.key
.env.ffm
sync_logs/

# ═══ Node.js ═══
node_modules/
.next/

# ═══ IDE ═══
.vscode/
.idea/
*.swp
*.swo

# ═══ Temp / System ═══
.tmp/
*.lock
*.log
logs/
.DS_Store
Thumbs.db

# ═══ Data (don't commit large data) ═══
agents/memory/chroma_db/
memory/data/*.jsonl
memory/data/chroma_store/

# ═══ Project Input ═══
project_input/bigquery_key.json
project_input/EXAMPLE_*/bigquery_key.json

# ═══ Gemini / Vercel ═══
.gemini/
.vercel/

# ═══ FAOS AI Brain Data ═══
data/inbox/
data/processed/
data/openfang/
data/openfang.db
data/stramark/

# ═══ Manual data dumps (PDFs, Excel exports, customer protocols) ═══
# Contains real customer/financial data — never commit
config/manual_data/

# ═══ V5.1 Cognitive Consultant - KEEP (AI can doc) ═══
# memory-bank/     <- KEEP (AI memory)
# .agents/         <- KEEP (V5.1 rules + workflows)
# .context/        <- KEEP (task context)
# AGENTS.md        <- KEEP (engine overview)
# REPO_GRAPH.md    <- KEEP (dependency graph)
# .cursorrules     <- KEEP (AI personality)
# docs/            <- KEEP (documentation)

# ═══ Per-Leader Local Files (personal, not shared) ═══
*.local.md
!*.local.md.example

# ═══ Auto Memory (AI-generated learnings) ═══
.auto-memory/*.md
!.auto-memory/INDEX.md.codenexus/
.worktrees
