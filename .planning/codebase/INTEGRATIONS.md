# INTEGRATIONS.md — External APIs & Services
> Auto-generated by gsd-map-codebase | AUUS1 / FAOS v6

## 1. Meta Marketing API (Facebook Ads)

- **Library**: `facebook-business>=20.0.0`
- **Purpose**: Fetch ad campaigns, adsets, ads performance data
- **Credentials per project** (env vars):
  - `AUUS1_META_APP_ID`, `AUUS1_META_APP_SECRET`, `AUUS1_META_ACCESS_TOKEN`
  - Also: `META_*`, `ZEN8_META_*`, `ZEN8_META_*`, `T1_META_*`, `TRENDIFY_META_*`, `HNLE_META_*`
- **Sync script**: `scripts/sync_meta_to_bq.py`, `scripts/sync_auus1_ads.py`
- **Ad Accounts** configured per project in `config/projects/`
- **FB Ads Library**: `scripts/sync_fb_library.py` → BigQuery ads spy tables

## 2. Google BigQuery

- **Library**: `google-cloud-bigquery==3.25.0`
- **Auth**: Service account `bigquery_key.json` (GOOGLE_APPLICATION_CREDENTIALS)
- **Project ID**: `levelup-465304`
- **Dataset**: `STRAMARK_Dataset` + project-specific datasets (e.g., AUUS1)
- **Usage**: Primary analytics data store — all ad metrics, orders, COGS, ROAS
- **SQL location**: `sql/auus1/`, `sql/v6/`, `sql/tables/`
- **Schema frozen**: `docs/SCHEMA_FROZEN.md` — DO NOT ALTER without approval

## 3. Poscake (Order Management / POS)

- **Base URL**: `https://pos.pages.fm/api/v1`
- **Purpose**: Order sync (sales, COD status, fulfillment)
- **Credentials per project**:
  - `AUUS1_POSCAKE_US_KEY`, `AUUS1_POSCAKE_AU_KEY` (US and AU markets)
  - Also: ZEN8, HNLE, TRENDIFY, TALPHA keys
- **Sync scripts**: `scripts/sync_auus1_orders.py` (27KB — most complex sync script)
- **Stock sync**: `scripts/sync_auus1_stock.py`

## 4. Telegram Bot

- **Purpose**: Alerts, action approvals, daily briefings
- **Credentials**: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`
- **Webhook server**: `faos_brain/webhook_server.py` — Handles Telegram callbacks
- **Director comms**: `faos_brain/director_comms.py`

## 5. Discord Webhooks

- **Purpose**: Reports, ETL alerts, system notifications
- **Webhooks** (env vars):
  - `DISCORD_WEBHOOK_REPORT` — Daily reports
  - `DISCORD_WEBHOOK_ALERT` — System alerts
  - `DISCORD_WEBHOOK_ETL` — ETL pipeline status
  - Per-project: `ZEN8_DISCORD_WEBHOOK`, `HNLE_DISCORD_WEBHOOK`, `TRENDIFY_DISCORD_WEBHOOK`

## 6. FalkorDB (Graph DB — AI Memory)

- **Library**: `falkordb>=1.0.0`
- **Backend**: Redis (`redis>=5.0.0`)
- **Deployed via**: `docker-compose.ai.yml`
- **Purpose**: Agent long-term memory, decision graphs, relationship storage
- **Integration**: `faos_brain/graph/`

## 7. N8N (Workflow Automation)

- **Config**: `N8N_HOST`, `N8N_API_KEY`
- **Purpose**: Workflow orchestration (ad-hoc automation tasks)

## 8. Botcake AI

- **Purpose**: Facebook Messenger chatbot for spiritual shop (Buddha statues)
- **Credentials**: `BOTCAKE_POSCAKE_API_KEY`, `BOTCAKE_POSCAKE_SHOP_ID`
- **Test**: `tests/test_inventory_api_botcake.py`

## 9. Google Gemini API

- **Key**: `GEMINI_API_KEY`
- **Endpoint**: via `google-generativeai` SDK
- **Primary model**: `gemini-3-flash-preview`

## 10. OpenAI API

- **Key**: `OPENAI_API_KEY`
- **Role**: Fallback when Gemini fails
- **Fallback chain**: `faos_brain/llm_client.py` → Gemini → GPT → Error

## Data Flow Summary

```
Meta API → sync scripts → BigQuery → FastAPI → Next.js Dashboard
Poscake  → sync scripts → BigQuery
Telegram ← marketing_director.py ← analyst.py ← BigQuery
FalkorDB ↔ faos_brain (agent memory layer)
```
