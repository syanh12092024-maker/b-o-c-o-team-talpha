# CONCERNS.md — Technical Debt & Issues
> Auto-generated by gsd-map-codebase | AUUS1 / FAOS v6

## 🔴 High Priority

### 1. Schema Frozen — BigQuery
- `docs/SCHEMA_FROZEN.md` exists indicating schema is **frozen**
- Any new feature requiring BQ schema changes needs careful planning
- **Risk**: New features may be blocked without proper schema review process

### 2. Hardcoded Project Paths in ecosystem.config.js
```javascript
cwd: "/Users/tatthanh031298/Desktop/AUUS/dashboard-ui",
```
- Absolute paths hardcoded to local machine
- **Impact**: Will fail on VPS deployment without manual edit
- **Fix needed**: Use relative paths or env-based config

### 3. No Frontend Tests
- `dashboard-ui/` has **zero automated tests** (no Jest, no Playwright)
- Growing UI complexity (13+ component directories, 24KB admin-home.tsx)
- Dashboard bugs are only caught manually
- **Risk**: Regressions not detected

### 4. Large Core Files
- `faos_brain/marketing_director.py`: 30KB — too large for single file
- `faos_brain/analyst.py`: 23KB — approaching complexity limit
- `scripts/sync_auus1_orders.py`: 27KB — needs splitting
- **Risk**: Hard to maintain, context rot when AI edits these files

## 🟡 Medium Priority

### 5. LLM Cost & Rate Limit Risk
- Primary LLM is Gemini (rate limits apply)
- Fallback to GPT-4o (cost risk if Gemini is consistently down)
- No token usage monitoring/budgeting found

### 6. `.env` Contains Sensitive Data in VPS
- `bigquery_key.json` is a service account key committed/copied to VPS
- Pattern: `GOOGLE_APPLICATION_CREDENTIALS=bigquery_key.json`
- **Risk**: Key rotation process not documented

### 7. Legacy SQL Accumulation
- `sql/_legacy/` directory with many `fix_*.py` and `fix_*.sql` files
- Suggests iterative patching pattern rather than clean migrations
- `sql/fix_zen8_*.py` files: 6 files, some 20-24KB
- **Risk**: Unclear which fixes were applied, no migration tracking

### 8. Multi-Project Config Sprawl
- 8+ projects (auus1, zen8, stramark, t1, hnle, talpha, trendify, pialpha)
- Each with separate env vars, ad accounts, BigQuery datasets
- No centralized project registry validation
- **Risk**: Adding new projects requires editing `.env` + config/ + dashboard-ui/

### 9. `start_dry_run_cycle.sh` (9.9KB)
- Large shell script as cron entry point
- Complex orchestration in bash (fragile)
- **Risk**: Difficult to test, debug failures

### 10. FalkorDB Dependency
- Graph DB runs via Docker locally
- `docker-compose.ai.yml` for local dev
- Production VPS setup: unclear if FalkorDB is stable/monitored
- If FalkorDB goes down, all agent memory operations fail

## 🟢 Low Priority / Observations

### 11. get-shit-done Repo in Project Root
- `get-shit-done/` directory at AUUS root is the cloned GSD repo
- Should probably be added to `.gitignore` or moved outside AUUS
- Not a codebase concern, just project hygiene

### 12. Python 3.14 Compatibility
- `requirements.txt` notes: `crewai` and `chromadb` disabled — incompatible with Python 3.14
- May need evaluation if upgrading Python version

### 13. Agentic-AI-Levelup/ Subdirectory
- Nested `Agentic-AI-Levelup/` at root — purpose unclear (training data? course material?)
- Could be confusing with the main project root

### 14. Missing `.env` Validation
- No startup validation that all required env vars are set
- Missing credentials discovered at runtime (silent failures risk)

## Architecture Concerns

### Context Window Management
- `marketing_director.py` (30KB) + prompts could hit LLM context limits
- GSD installation helps with this via fresh context per agent

### Data Pipeline Reliability
- Sync scripts run as standalone scripts (no retry logic visible)
- BigQuery upload failures may silently drop data

## Recommended Immediate Actions

1. Add `get-shit-done/` to `.gitignore`
2. Split `marketing_director.py` into focused modules  
3. Add at minimum smoke tests for Next.js dashboard
4. Document VPS deployment process for BQ key rotation
5. Consider schema migration tooling (alembic-style for BQ)
