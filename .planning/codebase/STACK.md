# STACK.md — Technology Stack
> Auto-generated by gsd-map-codebase | AUUS1 / FAOS v6

## Runtime & Languages

| Layer | Technology | Version |
|-------|-----------|---------|
| Python | Core AI, scripts, backend | 3.12 (`.venv`) |
| Node.js | Frontend, tooling | via npm |
| SQL | BigQuery DDL & Views | Google SQL |

## Backend Framework

- **FastAPI** `>=0.115.0` — REST API server
  - Entry point: `faos_brain/api/main:app`
  - Host: `0.0.0.0:8000` (Uvicorn)
  - SSE (Server-Sent Events) for real-time dashboard feed
- **Uvicorn** `>=0.30.0` — ASGI server
- **Pydantic** `>=2.9.0` — Data validation + settings
- **slowapi** `>=0.1.9` — Rate limiting
- **sse-starlette** `>=2.0.0` — SSE support

## Frontend Framework

- **Next.js 15** with **React 19**
  - Location: `dashboard-ui/`
  - Dev port: `3000`
  - Multi-project tabs: auus1, zen8, stramark, t1, hnle, talpha, trendify
  - Charting: **Recharts**
  - Started via PM2 (`auus1-frontend` process)

## AI / LLM

- **Google Gemini** (`google-generativeai>=0.8.0`) — Primary LLM
  - Default model: `gemini-3-flash-preview`
  - Configured via `GEMINI_API_KEY`
- **OpenAI GPT-4o** (`openai>=1.50.0`) — Fallback LLM
  - Fallback chain: Gemini → GPT → Error (in `faos_brain/llm_client.py`)
- **LLM Temperature**: `0.3`

## Data Layer

- **Google BigQuery** (`google-cloud-bigquery==3.25.0`)
  - Project: `levelup-465304`
  - Dataset: `STRAMARK_Dataset` (default), per-project datasets
  - Auth: Service account JSON (`bigquery_key.json`)
  - Schema frozen — see `docs/SCHEMA_FROZEN.md`
- **FalkorDB** (`falkordb>=1.0.0`) — Graph database (AI memory)
  - Runs via Docker (`docker-compose.ai.yml`)
  - Redis backend (`redis>=5.0.0`)
  - Used for agent long-term memory + relationship graphs

## Key Python Dependencies

```
google-cloud-bigquery==3.25.0
google-auth==2.34.0
pandas==2.2.2
facebook-business>=20.0.0
python-dotenv==1.0.1
requests==2.32.3
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.9.0
openai>=1.50.0
google-generativeai>=0.8.0
falkordb>=1.0.0
redis>=5.0.0
httpx>=0.27.0
apscheduler>=3.10.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
```

## Process Management

- **PM2** (`ecosystem.config.js`) — Production process manager
  - `auus1-frontend` — Next.js on port 3000
  - `auus1-backend` — Uvicorn FastAPI on port 8000
  - Logs: `logs/pm2-*.log`

## Infrastructure

- **Contabo VPS** (Ubuntu 22.04) — Production hosting
- **Docker** — FalkorDB + Redis services
- **Git** — Version control

## Configuration

- `.env` / `.env.example` — Environment variables
- `config/` — Per-project YAML configs
  - `config/projects/` — Project-specific settings
  - `config/naming_registry.yaml` — Facebook ads naming rules
  - `config/thresholds.yaml` — ROAS/CPA alert thresholds
  - `config/schedules.yaml` — Cron timing
- `ecosystem.config.js` — PM2 process config
